import React from "react";
import "./Title.css";

const Title = props => (
  <div className="titlearea">


    <p>Click on an image of a bird to score, but be careful not to click on the same image twice!</p>

  </div>
);
export default Title;
