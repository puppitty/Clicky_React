import React from "react";
import "./Title.css";

const Title = props => (
  <div className="titlearea">
    <div className="container">

      <p>Click on an image of a bird to score, </p>
      <p>but be careful not to click on the same image twice!</p>
    </div>

  </div>
);
export default Title;
